# This file is maintained automatically by "terraform init".
# Manual edits may be lost in future updates.
# Commit this file to version control — it pins provider versions
# so all team members and CI get the exact same provider binaries.

provider "registry.terraform.io/hashicorp/aws" {
  version     = "5.40.0"
  constraints = "~> 5.40"
  hashes = [
    "h1:placeholder-hash-replace-after-terraform-init",
  ]
}
